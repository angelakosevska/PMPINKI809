<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.INKI809" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.INKI809" parent="Base.Theme.INKI809" />

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@color/black</item>
        <item name="android:textColor">@color/white</item> <!-- Changes search text color -->
        <item name="android:textColorHint">#607373</item> <!-- Changes hint text color -->
    </style>

</resources>